#include <stdio.h>
/**
 * main - this is the main function to print with proper grammer
 *
 * Return: always 0 when correct
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
