#include <stdio.h>
/**
 * main - this is the main function
 *
 * Return: at all time 0 when successful
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
		       putchar(c);
	       c++;
		}
	putchar('\n');
	return (0);
}
