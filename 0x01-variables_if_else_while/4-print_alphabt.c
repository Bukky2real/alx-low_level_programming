#include <stdio.h>
/**
 * main - main Entry
 *
 * Return: always 0 when successful
 */

int main(void)
{
	char c;

	c = 'a';
while
	(c <= 'z')
		if ((c != 'q' && c != 'e') && c <= 'z')
			putchar(c);
				c++;
	putchar('\n');
	return (0);
}
